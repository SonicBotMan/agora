import { HermesRuntimeAdapter } from '../../main/libs/agentEngine/hermesRuntimeAdapter';

/**
 * Top-level architecture facade for the Hermes runtime adapter.
 */
export class HermesAdapter extends HermesRuntimeAdapter {}
