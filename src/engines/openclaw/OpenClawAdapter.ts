import { OpenClawRuntimeAdapter } from '../../main/libs/agentEngine/openclawRuntimeAdapter';

/**
 * Top-level architecture facade for the OpenClaw runtime adapter.
 */
export class OpenClawAdapter extends OpenClawRuntimeAdapter {}
